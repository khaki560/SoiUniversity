<Window x:Class="client.PopUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:client"
        mc:Ignorable="d"
        Title="PopUpWindow" Height="600" Width="500">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="45" />
			<RowDefinition Height="*" />
			<RowDefinition Height="30" />
		</Grid.RowDefinitions>


		<Label Name="LabelTitle" Content="asd"  HorizontalAlignment="Center" FontSize="20" Grid.Row="0"/>
		<TextBlock Name="LabelKey" Text="asd" TextWrapping="Wrap" Focusable="True"  FontSize="20" Grid.Row="1"/>


		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="250"/>
				<ColumnDefinition Width="250"/>
			</Grid.ColumnDefinitions>
			
			<Button  Grid.Column="1" Content="OK" Click="Button_Click" />
			<Button  Grid.Column="0" Content="Copy to clipboard" Click="ButtonClipboard_Click" />
		</Grid>



	</Grid>
</Window>
